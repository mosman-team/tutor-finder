export default class Info {
    constructor(id, city, address, price, keyWords) {
        this.id = id
        this.city = city
        this.address = address
        this.price = price
        this.keyWords = keyWords
    }
}